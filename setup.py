from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='trying out the cookiecutter template with fashion MNIST',
    author='Victor',
    license='',
)
